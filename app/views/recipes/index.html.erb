 <%= form_with url: recipes_path, method: :get, local: true, data: {turbo: false} do |f| %>
      <%= f.text_field :search, placeholder: "Search by Ingredients..." %>
      <%= f.submit "Search" %>
  <% end %>

<% @recipe_facade.recipes.each do |recipe| %>
  <ul class= "recipe">
  <li class="title"><%= recipe.name %>
  </ul>
<% end %>